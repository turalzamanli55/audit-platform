/**
 * Responsive design configuration.
 * Architecture placeholder — implementation deferred.
 */
export const responsiveStrategy = {
  approach: "desktop-first",
  targets: ["desktop", "laptop", "tablet", "ipad", "android", "iphone"],
} as const;
