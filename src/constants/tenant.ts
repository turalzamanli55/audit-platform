/**
 * Tenant preference cookies — active organization and workspace selection.
 */
export const TENANT_COOKIES = {
  organizationId: "audit-organization-id",
  workspaceId: "audit-workspace-id",
} as const;

export const ONBOARDING_PATH = "/app/onboarding";
export const DASHBOARD_PATH = "/app/dashboard";
