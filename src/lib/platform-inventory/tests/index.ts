/**
 * EPIRE tests facet — inventory signals consumed via EPBSE/EPAC/ECIE/EIIE (no duplicated scans).
 */
export { buildInventoryCounts } from "@/lib/platform-inventory/inventory";
export { buildModuleInventory } from "@/lib/platform-inventory/modules";
